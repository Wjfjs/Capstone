aws db 아이디,비밀번호
admin
asdf1234

연결 주소
host: 220.69.36.251/32;
user:admin;
password:asdf1234;
database:mydatabase;


db 테이블과 테이블열들 이름, 제약조건
CREATE TABLE Road (
    RoadNumber INT PRIMARY KEY,
    TrafficVolume INT,
    Time DATETIME,
    Date DATE
);

CREATE TABLE Log (
    LogNumber INT PRIMARY KEY,
    ChangeTime DATETIME,
    ChangeContent TEXT
);

//차량 카운트 기록
create table count (
	id int AUTO_INCREMENT PRIMARY KEY,
    date date,
    count int default 0
);

CREATE TABLE TrafficLight (
    TrafficLightID INT PRIMARY KEY AUTO_INCREMENT,
    CurrentState VARCHAR(100)
);

닉네임 rinotom
이메일 rhkddns2084@gmail.com
나머지 minerva

git clone -b test --single-branch https://github.com/Wjfjs/Capstone.git


brench 확인 방법
git branch

brench 교체 방법
git checkout test

연결 확인
remote

연결
git remote add Minerva https://github.com/Wjfjs/Capstone.git

접속(ssh 생성)
ssh-keygen -t rsa -b 4096 -C "rhkddns2084@gmail.com"
